apiVersion: v1
kind: Service
metadata:
  name: kubernetes-mcp
  namespace: kubernetes-mcp
  labels:
    app: kubernetes-mcp
spec:
  type: ClusterIP
  selector:
    app: kubernetes-mcp
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
