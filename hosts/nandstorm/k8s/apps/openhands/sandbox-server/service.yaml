apiVersion: v1
kind: Service
metadata:
  name: openhands-sandbox-server
  namespace: openhands
  labels:
    app.kubernetes.io/name: sandbox-server
    app.kubernetes.io/part-of: openhands
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: sandbox-server
