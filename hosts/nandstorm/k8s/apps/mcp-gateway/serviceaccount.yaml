apiVersion: v1
kind: ServiceAccount
metadata:
  name: mcpgateway-sa
  namespace: mcp-gateway
---
# Used when the gateway deploys tool/server pods with workload identity enabled.
# (Left here to match upstream local deployment manifest.)
apiVersion: v1
kind: ServiceAccount
metadata:
  name: workload-sa
  namespace: mcp-gateway
